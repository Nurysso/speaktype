{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window and the recorder pill",
  "windows": [
    "main",
    "pill"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:menu:default",
    "opener:default",
    "dialog:allow-open"
  ]
}
